#include<stdio.h>
#include<stdlib.h>
int char_int(char x)
{
    return (int)(x-96);
}
main()
{
    int graph[5][5];
    int i,j;
    char a,b;
    for(i=0;i<5:i++)
        for(j=0;j<5;j++)
        {
            graph[i][j]=0;
        }

printf("enter the edge between(a-e)");
scanf("%c %c",&a,&b);


}